# Dependencies & packaging
node_modules/
*.vsix

# Build output (regenerated by `npm run build`)
dist/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/

# Design sources not needed for rebuild
*.ai
*.psd

# Keep generator sources versioned: src/, scripts/, *.ts, tsconfig, templates
# (packaging excludes them via .vscodeignore — pack ≠ VCS)
